# 인스타 자동 게시 — 지금 어떻게 되어 있나 (2026-09-08 갱신)

계정 `@bdai_79` · 하루 한 편 · **매일 저녁 7시** 자동 발행.

---

## 오늘 바뀐 것

### 1. 멈춰 있던 원인을 찾아 고쳤습니다

8/27 이후 예약 실행이 **한 번도 글을 올리지 못했습니다.**
작업 스케줄러는 "성공"이라고 표시했지만, 실제로는 아무 일도 일어나지 않았습니다.

원인: 중간에 끼어 있던 `run_silent.vbs`(VBScript)가 더 이상 실행되지 않습니다.
윈도우가 이 방식을 막았습니다. 스케줄러는 vbs를 띄우자마자 "성공"으로 처리하기 때문에
겉으로는 정상으로 보였습니다.

**고친 방법**: 스케줄러가 `run_post.bat` 을 **직접** 실행하도록 바꿨습니다. vbs를 거치지 않습니다.
바꾼 뒤 예약 실행을 실제로 돌려 확인했습니다(게시는 하지 않는 시험용으로).

### 2. 테스터 모집 글이 들어갔습니다

`content_plan.json` 에 테스터 모집 글 3편이 추가됐습니다.
`campaign` 표시가 붙은 글은 **일반 30일 글보다 먼저** 나갑니다.

| Day | 주제 | 이미지 |
|---|---|---|
| 101 | 정직한 카운트 (스쿼트 100번 → 62번) | 운동 횟수 AI 배너 |
| 102 | 20명 한정 모집 | 테스터 모집 배너 |
| 103 | 불편한 점만 말해 주세요 | 같이 테스트하실 분 배너 |

모집 조건은 신청 페이지·초대 메일과 **같은 문구**를 씁니다 —
`14일 사용 + 피드백 한 번 → 운동 스킨 + UI 스킨 전부 지급`.

인스타 글에는 **누를 수 있는 링크를 넣을 수 없습니다.** 그래서
`댓글에 [참여] → DM으로 안내` 방식입니다. (DM 감시 도구가 `dm_manager` 에 있습니다)

### 3. 미리보기와 진단 도구

```bash
python auto_post/post_daily.py --dry-run
```
무엇이 올라갈지 보여주기만 합니다. **아무것도 게시하지 않습니다.**

```bash
python auto_post/diagnose.py
```
토큰이 살아 있는지, 실제 피드에 무엇이 있는지, 다음에 무엇이 올라갈지 알려 줍니다. 읽기 전용입니다.

### 4. 발행 기록 보정

실제로 올라갔는데 로컬 기록에 빠져 있던 2건(Day 4, Day 7)을 채웠습니다.
그대로 뒀으면 **같은 글이 두 번 올라갈 뻔했습니다.**
원본은 `post_log.backup-20260908.json` 에 있습니다.

---

## 쓰는 법

| 하고 싶은 것 | 명령 |
|---|---|
| 다음에 뭐가 올라가는지 본다 | `python auto_post/post_daily.py --dry-run` |
| 지금 바로 한 편 올린다 | `python auto_post/post_daily.py` |
| 왜 안 올라가는지 본다 | `python auto_post/diagnose.py` |
| 새 이미지를 올린다 | `python auto_post/upload_images.py <파일>` |
| 모집 배너를 새로 만든다 | `E:\무브카운터 관리\scripts\generate_ad_banner.py` |

## 알아 둘 제약

| | |
|---|---|
| 하루 한 편 | 같은 날 두 번 올리지 않도록 막혀 있습니다 |
| 이미지 필수 | 인스타는 글만으로는 못 올립니다. 공개 https 주소여야 합니다 |
| 링크 불가 | 캡션의 주소는 눌리지 않습니다. 프로필 링크나 DM으로 유도합니다 |
| 토큰 수명 | 60일. 만료되면 `diagnose.py` 가 알려 줍니다 |
| 캡션 한도 | 2200자 |

## 아직 안 된 것

- **프로필(바이오) 링크** — API로는 바꿀 수 없습니다. 인스타 앱에서 직접 넣으셔야 합니다.
  넣을 주소 후보: 테스터 그룹 `https://groups.google.com/g/movecounter-testers`
- **신청 페이지가 인터넷에 없습니다.** `E:\무브카운터 관리\apply` 는 지금 이 PC 안에서만 열립니다.
  공개 주소가 생기면 프로필 링크를 그쪽으로 바꾸는 게 낫습니다.
- 댓글 [참여] 에 자동으로 DM 보내기는 아직 사람이 합니다.

## 보안 메모

`run_post.bat` 안에 접속 토큰이 **그대로 적혀 있습니다.** 이 저장소를 공개하면 안 됩니다.
`.env` 에 이미 같은 값이 있으므로, 나중에 bat 에서는 지우는 편이 안전합니다.
